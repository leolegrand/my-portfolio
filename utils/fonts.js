import { Poppins, Playfair_Display } from 'next/font/google';

export const PlayfairDisplay = Playfair_Display({ subsets: ['latin'], weight:['400'] });
export const poppins = Poppins({ subsets: ['latin'], weight: ['200','400'] });